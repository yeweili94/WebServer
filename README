基于reactor模式，epoll水平触发的http服务
双缓冲异步日志
valgrind检测内存泄漏
依赖boost库(不打算改用c++11)

待改进:
1.线程池线程动态减少
2.任务队列可以采用无锁队列,避免加锁开销
3.日志分等级处理

使用方式：
编译之前确认已经安装boost:
sudo apt-get install libboost-dev

mkdir build;
cd build; cmake .. && make
cd bin

enjoying...
